enum HiveBox {
  AUTH('auth'),
  DICTIONARY('dictionary');

  const HiveBox(this.name);

  final String name;
}

enum HiveStorageKey {
  AUTH('auth'),
  DICTIONARY('dictionary');

  const HiveStorageKey(this.name);

  final String name;
}
